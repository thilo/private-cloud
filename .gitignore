# Secrets and generated runtime config — never commit
.env
.env.production
# Setup-only secrets (initial admin accounts), generated by scripts/init.sh
.env.setup
.env.production.setup
secrets/

# Local CA export / scratch
caddy/root.crt
caddy-root.crt
*.tmp
*.bak

# Verification scratch
scripts/.verify-tmp/

# Claude Code local (personal) settings
.claude/settings.local.json
